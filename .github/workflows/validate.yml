name: Validate

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  hassfest:
    name: hassfest
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v4.2.2

      - name: Run hassfest
        uses: home-assistant/actions/hassfest@868e6cb4607727d764341a158d98872cd63fa658 # master (2026-04-19)

  hacs:
    name: hacs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v4.2.2

      - name: Run HACS validation
        uses: hacs/action@dcb30e72781db3f207d5236b861172774ab0b485 # main (2026-04-19)
        with:
          category: integration
          # Brand icons ship locally under custom_components/arbor/brand/ via
          # the HA 2026.3 bundled-brands mechanism
          # (https://developers.home-assistant.io/blog/2026/02/24/brands-proxy-api/).
          # home-assistant/brands no longer accepts PRs for custom integrations,
          # so the HACS CDN-backed brands check cannot pass; keep it ignored.
          ignore: brands
